/** @type {import('tailwindcss').Config} */
export default {
  darkMode: "class",
  theme: { extend: {} },
  // v4 doesn't need `content` here; it auto-detects.
  // Remove plugins here; plugins are now added via `@plugin` in CSS.
};
